# Timesheet Rebuild Step 7.11.5: タイムシート補助UI整理

## 目的

タイムシート実装の進行で増えた小ウィンドウを整理し、作画中の画面を散らかさないようにする。

## 変更

- `タイムシート再生`、`タイムシート保存`、`タイムシート解決結果`、`原画間検出` を `タイムシート補助` ウィンドウへ集約した。
- セクションは以下の順に並べる。
  - T再生 / 作画対象
  - 保存 / 読み込み
  - タイムシート解決結果
  - 原画間検出 / 中割 / ライトテーブル
- 既存の再生、T範囲再生、中割nコマ打ち、ライトテーブル反映、編集F優先表示の挙動は変えない。

## 今回やらないこと

- Scene Plate表示UI
- Scene Plate画像読み込み
- PNG/MP4出力接続
- Project保存との正式統合

## 確認

- `タイムシート補助` ウィンドウが1つ出る。
- 旧小ウィンドウが重複して出ない。
- タイムシート再生、T範囲再生、中割nコマ打ち、ライトテーブル反映が従来通り使える。
