# final_spec_v6 Phase 2 Step 2-r: project signature selftest

- Add a focused selftest for `appio::projectSignature()`.
- Verify that metadata, motion keys, Fill bitmap samples, and point pressure affect the signature.
- Keep the test independent of the full app UI.
- Use this as a guard for future save/load field additions.
